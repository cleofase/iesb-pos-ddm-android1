<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.cleofase.news">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presenter.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presenter.LoginActivity" />
        <activity android:name=".presenter.SignupActivity" />
        <activity android:name=".presenter.ResetPassActivity" />
        <activity android:name=".presenter.UserDetailActivity" />
        <activity android:name=".presenter.NewDetailActivity" />
        <activity android:name=".presenter.UserNewsActivity"></activity>
    </application>

</manifest>